i should move to unions in some of the internal filesystem structs
i should also fix the makefile headers

so we'll have rootstreams
todo: prevent two processes from opening same inode at once
todo: make write calls able to resize inodes

kernel buffering


disk scanning
abstraction over inode
	file cache
	drive numbers
make UPALLOC zero-initialise
close stdios and streams and shit when a prockses clsoerns
	hack done-ish
figure out /dev so we can do /dev/tty or /dev/com or whatnot
	hack done: made a stub for if the directory is /dev
user privs, directories
process errors
